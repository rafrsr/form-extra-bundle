{% block rafrsr_date_time_picker_widget %}
    {{ form_widget(form) }}
    <script>
        $(function () {
            var picker = $('#{{ form.vars.id }}');
            {% if mopa_enabled %}
                picker.closest('div').datetimepicker({{ widget_options|raw }});
                picker.closest('div').addClass('date');
            {% else %}
                picker.datetimepicker({{ widget_options|raw }});
            {% endif %}
        });
    </script>
{% endblock %}